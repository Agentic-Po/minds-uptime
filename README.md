# uptime

A small scheduled availability probe. Runs on GitHub Actions, records a compact
status file, and sends notifications to a private channel. All targets,
credentials, and identifiers are supplied via encrypted Actions secrets; this
repository contains only generic method code and unlabeled numeric state.
